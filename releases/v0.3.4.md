# v0.3.4

**Released:** 2026-06-13

Changes since v0.3.3 (`bbd1b17`..`ba11099`).

## Dependencies

- aws-sdk-s3 1.225.0 -> 1.225.1
- brakeman 8.0.4 -> 8.0.5 (dev/test)
- ruby_llm 1.15.0 -> 1.16.0 (adds opt-in concurrent tool execution, Rails-style instrumentation, per-provider `api_base`; defaults unchanged; OpenRouter monkey-patch verified still applies)

## Infrastructure

- Bumped 5 `docker/*` GitHub Actions in `docker-publish.yml` to their latest majors (setup-qemu v4, setup-buildx v4, login v4, metadata v6, build-push v7). First real exercise is this release.

## Test Coverage

- 450 Ruby tests, 1,393 JavaScript tests (1,843 total) — unchanged from v0.3.3.
